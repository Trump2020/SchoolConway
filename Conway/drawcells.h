#pragma once
#include <iostream>
#include <Windows.h>

double findcellcord(cells[x], cells[y]) {
	
}
void drawcells() {
	double findcellcord()
	rectangle(cells[x][y], cells[x][y], cells[x][y] + scl, cells[x][y] + scl)
}